steps to run the node.js app- 

1. install node.js (if not present) from [node.js](https://nodejs.org/)
2. open terminal in the folder "nodetable"
3. run the command "npm init -y"
4. run the command "npm install express"
5. run the command "node server.js"
6. open browser and go to "http://localhost:3000"